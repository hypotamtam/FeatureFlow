name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-package-ios:
    name: Test Swift Package (iOS)
    runs-on: macos-26
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.4'

      - name: Run Package Tests (iOS)
        run: |
          xcodebuild test \
            -scheme FeatureFlow-Package \
            -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.2' \
            -only-testing FeatureFlowTests \
            OTHER_SWIFT_FLAGS="-strict-concurrency=complete" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

  test-package-macos:
    name: Test Swift Package (macOS)
    runs-on: macos-26
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.4'

      - name: Run Package Tests (macOS)
        run: |
          xcodebuild test \
            -scheme FeatureFlow-Package \
            -destination "platform=macOS" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

  build-demo-ios:
    name: Build Demo (iOS)
    runs-on: macos-26
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.4'

      - name: Build iOS Demo
        run: |
          cd Demo
          xcodebuild clean build \
            -scheme Demo \
            -destination "platform=iOS Simulator,name=iPhone 17,OS=26.2" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

  build-demo-macos:
    name: Build Demo (macOS)
    runs-on: macos-26
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.4'

      - name: Build macOS Demo
        run: |
          cd Demo
          xcodebuild clean build \
            -scheme Demo \
            -destination "platform=macOS" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

  test-demo-ios:
    name: Test Demo (iOS)
    runs-on: macos-26
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.4'

      - name: Run Demo Tests
        run: |
          cd Demo
          xcodebuild clean test \
            -scheme Demo \
            -destination "platform=iOS Simulator,name=iPhone 17,OS=26.2" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO
